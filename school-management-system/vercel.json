{
  "buildCommand": "npm run build",
  "outputDirectory": "classroom-frontend/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" },  
    { "source": "/(.*)", "destination": "/" }    
  ]
}